# Dodenakkers: ontwerp- en onderzoeksnotities

Deze map bevat losse Markdown-notities voor ideeën, data-audits en MVP-beslissingen.

## Kerncijfers

*Bijgewerkt bij elke build (`scripts/build_base_dataset.py` +
`scripts/analyse_spatial.py`); zie de onderliggende audits voor detail en
methodiek: [Auditresultaten basisdataset](data/kml-audit-resultaten.md) en
[005 Erfgoedrelaties resultaten](data/005-erfgoedrelaties-resultaten.md).*

**Begraafplaatsen** - 448 terreinen, samen circa **520 ha**:

- 401 niet-geruimd, 47 geruimd, 0 statusconflicten (de eerdere 4 zijn op 2026-08-20 door Leon bevestigd als geruimd en opgelost in de bron, zie [003 CSV als genormaliseerde bronlaag](data/003-csv-bron-en-koppeling.md#statusconflicten-2026-08-20-opgelost); 2 losstaande correcties -- Oudenhoorn/Gem. begraafplaats en Stad aan 't Haringvliet/NH kerkhof -- kwamen er op 2026-08-22 bij van de opdrachtgever zelf);
- kleinste terrein 13,49 m², grootste 268.823,91 m² (26,9 ha), mediaan 3.177,55 m².
- was 463 tot 2026-08-19: 20 begraafplaatsen in Vijfheerenlanden-dorpen (nu provincie Utrecht, niet Zuid-Holland) verwijderd uit de bron, zie [003 CSV als genormaliseerde bronlaag](data/003-csv-bron-en-koppeling.md#vijfheerenlanden-2026-08-19-bronwijziging);
- 443 -> 446 op 2026-08-23: 3 begraafplaatsen (Nieuwe Joodse begraafplaats Schiedam, Grafmonument juffrouw Begeer Voorschoten, NH Kerkhof Oud-Alblas) toegevoegd uit Leons "Tijdelijk Zuid-Holland.kmz", zie [003 CSV als genormaliseerde bronlaag](data/003-csv-bron-en-koppeling.md#tijdelijk-zuid-holland-kmz-2026-08-23-bronwijziging);
- 446 -> 448 op 2026-08-26: nog 2 begraafplaatsen (NH kerkhof, De Lier; NH Kerkhof, Oostvoorne; beide geruimd) toegevoegd uit een tweede "Tijdelijk Zuid-Holland"-kmz van Leon, zie [003 CSV als genormaliseerde bronlaag](data/003-csv-bron-en-koppeling.md#tijdelijk-zuid-holland-2-kmz-2026-08-26-bronwijziging).

**Tegenover beschermde gezichten** (105 rijksbeschermde stads-/dorpsgezichten in de Zuid-Holland-bbox):

- 43 begraafplaatsen liggen volledig binnen een beschermd gezicht;
- 5 overlappen een beschermd gezicht deels;
- 400 hebben geen relatie met een beschermd gezicht.

**Tegenover archeologische rijksmonumenten** (99 in de bbox):

- 0 begraafplaatsen overlappen een archeologisch rijksmonument;
- dichtstbijzijnste niet-overlappende geval: NH kerkhof, Heenvliet, **6,2 m** van een archeologisch rijksmonument - een randgeval, geen "ver weg", zie sectie "Bijna-overlap" in de erfgoedrelaties-audit.

**Tegenover (gebouwde) rijksmonumenten** (14.204 in de bbox):

- 247 begraafplaatsen hebben minstens één rijksmonument binnen 100 m (316 binnen 250 m -- de dataset bewaart relaties tot 250 m zodat de schuifregelaar in de viewer verder dan 100 m kan verkennen; categorieën `inside_on_site`/`touches`/`intersects`/`0-25m`/`25-100m`/`100-250m` - voorlopige werkhypothesen, ruwe afstand blijft altijd bewaard).

Los daarvan, als voorbeeld van wat de functiefilter in de viewer oplevert (geen vaste systeemcategorie, gewoon een zoekopdracht op het echte RCE-label): 65 van de 14.204 rijksmonumenten in de bbox hebben een oorspronkelijke functie waar "begraafplaats" of "kerkhof" in voorkomt - ongeacht of ze bij een van de 448 begraafplaatsen in de buurt liggen.

**Archeologische onderzoeksgebieden** (`ceo:ArcheologischOnderzoeksgebied`, andere class dan rijksmonumenten, wens van de gebruiker, 2026-08-20): 22.254 gebieden in de bbox gepubliceerd als losse, standaard uitgeschakelde kaartlaag (200 gebieden met een "vertrouwelijk"-vlag in de bron zijn uitgesloten, zie [004 RCE-MCP querystrategie](data/004-rce-mcp-querystrategie.md#q4-archeologische-onderzoeksgebieden-2026-08-20)). Geen spatiale koppeling met de begraafplaatsendataset (nog niet gevraagd), puur een informatieve referentielaag.

## Ideeën

- [001 Architectuur](ideas/001-architectuur.md)
- [002 Viewer](ideas/002-viewer.md)
- [003 Ruimtelijke analyse](ideas/003-ruimtelijke-analyse.md)
- [004 RCE Linked Data](ideas/004-rce-linked-data.md)
- [005 Kadaster en PDOK](ideas/005-kadaster-pdok.md)
- [006 Hosting](ideas/006-hosting.md)

## Data

- [001 Audit van de KML](data/001-kml-audit.md)
- [002 Datamodel begraafplaatsen](data/002-datamodel.md)
- [003 CSV als genormaliseerde bronlaag](data/003-csv-bron-en-koppeling.md)
- [004 RCE-MCP querystrategie](data/004-rce-mcp-querystrategie.md)
- [Auditresultaten basisdataset](data/kml-audit-resultaten.md)
- [005 Erfgoedrelaties resultaten](data/005-erfgoedrelaties-resultaten.md)
- [006 ABR2-thesaurus verkenning](data/006-abr-thesaurus-verkenning.md)

## MVP

- [001 Eerste MVP](mvp/001-eerste-mvp.md)

## Belangrijkste huidige besluiten

- de genormaliseerde CSV is de primaire build-bron; de KML blijft oorspronkelijke kaartbron;
- polygoon = terrein van de begraafplaats;
- punt = ingang/toegang;
- oppervlakte en omtrek worden uit de terreinpolygoon berekend;
- `geruimd` wordt als expliciet statusveld gemodelleerd;
- statusconflicten tussen punt en polygoon worden gerapporteerd en niet stil gecorrigeerd;
- RCE-extracten (beschermde gezichten, rijksmonumenten, archeologische rijksmonumenten) worden opgehaald via `scripts/fetch_rce.py` met opgeslagen SPARQL in `queries/rce/`, zie [004 RCE-MCP querystrategie](data/004-rce-mcp-querystrategie.md);
- de ruimtelijke join tussen begraafplaatsen en de RCE-extracten draait via `scripts/analyse_spatial.py` (output `data/generated/analyse.geojson`), zie [005 Erfgoedrelaties resultaten](data/005-erfgoedrelaties-resultaten.md);
- een eerste MapLibre-viewer (`src/index.html`/`app.js`) toont terrein, ingangen, beschermde gezichten en rijksmonumenten met filters op geruimd/statusconflict/erfgoedrelaties en een doorzoekbaar filter op oorspronkelijke functie (dynamisch opgebouwd uit de data, geen vaste lijst), tegen de PDOK BRT-achtergrondkaart (grijs) als ondergrond, met een korte introtekst en het Dodenakkers-logo bovenaan het paneel voor Leon;
- de provinciegrens van Zuid-Holland (`scripts/fetch_provinciegrens.py`, PDOK bestuurlijkegebieden WFS, opgeslagen in `data/pdok/`) ligt als toggelbare referentielijn onder alle andere lagen, puur ter oriëntatie;
- archeologische onderzoeksgebieden (`ceo:ArcheologischOnderzoeksgebied`, 22.254 in de bbox) zijn een losse, standaard uitgeschakelde laag die pas bij het aanzetten wordt opgehaald (17MB), zie [004 RCE-MCP querystrategie](data/004-rce-mcp-querystrategie.md#q4-archeologische-onderzoeksgebieden-2026-08-20);
- `scripts/export_statusconflicten.py` exporteert eventuele `geruimd`-statusconflicten naar `data/generated/statusconflicten.csv` (met CSV-regelnummers) zodat Leon ze handmatig kan annoteren; de 4 die zich hebben voorgedaan zijn op 2026-08-20 opgelost, het bestand is nu leeg;
- de filters in het paneel zijn twee groepen met andere combinatielogica (2026-08-23): de drie status-checkboxes (geruimd/niet-geruimd/statusconflict) verbreden de selectie (OR/unie) omdat het elkaar uitsluitende toestanden van hetzelfde veld zijn, de drie erfgoed-checkboxes versmallen (AND) omdat een begraafplaats meerdere erfgoedrelaties tegelijk kan hebben -- zie de toelichting bij `applyFilters()` in `src/app.js`;
- het paneel heeft een naam/plaats-zoekveld (los van de facet-filters, versmalt er altijd bovenop) en de secties zijn inklapbaar (`<details>`), met Zoeken/Ondergrond/Lagen/Filters standaard open en Functie/Legenda standaard dicht om het paneel compacter te maken;
- de legenda dimt items waarvan de bijbehorende laag uit staat, zodat de legenda meteen laat zien wat er op de kaart te zien is;
- Oudenhoorn had kortstondig een terrein met twee eigen ingangen (`EXTRA_INGANG_EXCEPTIONS`), maar Leon draaide de geruimd-status en de ingangtoewijzing dezelfde dag om na het zien van de live kaart; sindsdien heeft elk terrein weer precies 1 ingang, zoals de rest van de dataset, zie [003 CSV als genormaliseerde bronlaag](data/003-csv-bron-en-koppeling.md#oudenhoorn-2026-08-23-opgelost----in-twee-stappen-tweede-stap-definitief).
- twee viewer-fixes uit Leons/Renes feedbacklijst (2026-08-26): de ingang-popup toont niet langer de kale boolean `Gedeelde ingang: false`, maar een leesbaar `Type`-veld (`Hoofdingang` of `Gedeeld (ook ingang van naburig terrein)`); de rijksmonumentlagen renderen nu boven de terreinvlak-fill (`map.moveLayer(...)` zonder tweede argument) zodat de puntmarkers niet verkleuren onder het halftransparante vlak, gemeld door Rene (kleurenblind).
- WCAG 2.1 AA-audit (2026-08-26, `axe-core` headless-scan + handmatige contrastcontrole): 3 echte issues opgelost -- de rijksmonument-afstandsslider (`#rm-threshold`) had geen toegankelijke naam (nu `aria-label`); `.hint`- en `.count`-tekst (`#999`/`#888` op wit, resp. 2,85:1 en 3,54:1) haalden de AA-drempel van 4,5:1 niet, nu `#767676` (4,54:1); de MapLibre-attributielinks rechtsonder waren alleen via kleur te onderscheiden van omringende tekst (2,02:1, SC 1.4.1), nu met underline. Geen andere echte violations gevonden; axe's resterende "incomplete" color-contrast-melding is een bekende beperking bij het halftransparante paneel bovenop de canvaskaart, niet een echt contrastprobleem (handmatig met de gebruikte hexkleuren tegen wit geverifieerd).
- security- en UX-ronde (2026-08-26): `_headers` toegevoegd (Cloudflare Pages leest dit uit de root van `site/`, zie `scripts/build_site.py`) met een CSP (`script-src`/`style-src` beperkt tot `'self'` + `unpkg.com`, `img-src`/`connect-src` beperkt tot `'self'` + `service.pdok.nl`, `worker-src`/`child-src blob:` voor MapLibre's workers, `frame-ancestors 'none'` tegen clickjacking naast `X-Frame-Options: DENY`), geverifieerd met een lokale server die de echte headers serveert (geen CSP-violations bij alle bestaande interacties); handmatige controle van `maplibre-gl@4.7.1` (geen npm/CDN-package, dus geen `npm audit`) leverde geen bekende CVE's op (Snyk-advisorydatabase), wel is de laatste versie inmiddels 6.6.0 -- een major-upgrade valt buiten deze ronde, geen acute reden om 'm nu te forceren. Permalink toegevoegd: kaartweergave (center/zoom), ondergrond, laagtoggles, filters, functieselectie en zoekopdracht staan nu in de URL (`history.replaceState`, geen extra terug-geschiedenis) en worden bij het laden teruggezet. Data-export toegevoegd: knop om de huidige gefilterde selectie als CSV of GeoJSON te downloaden. Permalink-kopieerknop en beide exportknoppen zijn eigen kaart-controls rechtsboven (onder de zoom-knoppen), niet in het filterpaneel, om dat niet voller te maken.
- paneel-/popup-opschoning (2026-08-26, wens van Joop): `Koppelwijze ingang` en `ID` weg uit het begraafplaats-popup, `Geometriebron` weg uit het rijksmonument-popup -- alle drie interne technische velden zonder betekenis voor Leon/Rene. Typografie in het paneel teruggebracht van 6 door elkaar lopende tekstgroottes (16/13/12.5/12/11.5/11px, organisch gegroeid) naar 3: 16px voor de titel, 13px (`#panel`'s basis) voor leestekst (labels, checkboxes), 11px voor alle "meta"-tekst (subtitel, hints, tellingen, kopjes, invoervelden, statusregel). `#panel input/select/button` krijgen nu expliciet `font-family: inherit` -- formuliervelden erven dat anders niet van `body` en konden in het systeem-UI-font afwijken van de rest van de tekst.
- statistiekenpagina (2026-08-26, wens van Joop: "zoveel mogelijk overzichten, tabellen enzo"): `src/statistieken.html`/`statistieken.js`, gelinkt vanaf het hoofdpaneel. `scripts/compute_statistics.py` berekent alles vooraf naar `data/generated/statistieken.json` (zelfde scheiding rekenen/tonen als de rest van de site) -- basisstatistieken, per-plaats-tabellen (meeste begraafplaatsen/geruimd/oppervlakte), beschermde gezichten, rijksmonumenten-nabijheid (incl. meest voorkomende functie in de buurt), archeologie en ingangen. Twee tabellen buiten de bestaande 250m-relaties om, met een eigen ongelimiteerde `STRtree.nearest()`-berekening zoals `scripts/analyse_spatial.py`: nabijheid tot een molen (318 in de bbox, functiecategorie `Molen`/`Industrie- en poldermolen`/`Korenmolen`/`Ondermolen`/`Bovenmolen`/`Boezemmolen`) en tot een kasteel/buitenplaats (118). Ontdekt tijdens het bouwen: **22 begraafplaatsen zijn zelf (deels) een rijksmonument** (functie `Begraafplaats`/`-hek`/`-aula`/`Dierenbegraafplaats` op/grenzend aan het terrein) -- een eigen tabel. Bijvangst: 2 plaats-veldfouten in de bron gevonden en gecorrigeerd, zie [003 CSV als genormaliseerde bronlaag](data/003-csv-bron-en-koppeling.md#twee-plaats-velden-gecorrigeerd-2026-08-26-bijvangst).
- echte gemeentegrenzen (2026-08-27, wens van Joop): de bron heeft alleen "plaats" (dorp/stad), geen gemeente, en na herindelingen hoort een plaats niet altijd bij de gemeente die zijn naam doet vermoeden (Oudenhoorn -> Voorne aan Zee). `scripts/fetch_gemeentegrenzen.py` haalt de 50 Zuid-Holland-gemeenten op uit dezelfde PDOK bestuurlijkegebieden-WFS als de provinciegrens (`data/pdok/gemeenten-zuid-holland.geojson`); `scripts/analyse_spatial.py` voegt daarmee een `gemeente`-property toe via puntligging (representative_point, met nearest-fallback voor randgevallen -- in de praktijk 0 van de 448 nodig). Alle 448 begraafplaatsen kregen direct een treffer. `gemeente` staat nu ook in het begraafplaats-popup (na "Plaats") en de statistiekenpagina heeft een "Per gemeente"-sectie naast "Per plaats". De gemeentegrenzen zijn ook een toggelbare kaartlaag (`toggle-gemeentegrenzen`, lazy geladen zoals de archeologische onderzoeksgebieden -- 2,7MB, standaard uit) -- dunner/lichter gestileerd dan de provinciegrens (`#868e96` gestippeld i.p.v. `#495057` gestreept) zodat de twee niveaus duidelijk te onderscheiden blijven.
- vier kleinere verbeteringen op verzoek van Joop na het mergen van de vorige ronde (2026-08-27): (1) de "Statusconflict"-filter/legenda-item is uit het paneel gehaald nu de bron 0 conflicten meer heeft (een lege checkbox die nooit iets oplevert) -- het onderliggende `status_conflict`-veld, de gele/rode terreinkleur en de popup-tekst blijven wel bestaan als stil vangnet mocht een toekomstige bronupdate er weer een introduceren; `STATUS_FILTER_IDS` in `src/app.js` heeft dus nu 2 in plaats van 3 elkaar uitsluitende toestanden. (2) Een "Denominatie"-tabel op de statistiekenpagina: de bron heeft geen apart denominatieveld, dus `scripts/compute_statistics.py` classificeert op basis van trefwoorden in `naam` (RK/Rooms-Katholiek, NH/Hervormd, Gereformeerd, Joods, Doopsgezind, Evangelisch-Luthers, Remonstrants; geverifieerd dat geen enkele naam op 2+ patronen tegelijk matcht) -- 170 van de 448 (38%) kregen zo een denominatie, de rest ("Onbekend/algemeen", 278) zijn vaak juist gemeentelijke/algemene begraafplaatsen zonder kerkelijke binding, geen ontbrekend gegeven. (3) Een "nabijheid tot een kerk"-tabel, met dezelfde `nabijheid_tot_categorie()`-berekening als molen/kasteel (functiecategorie `Kerk`/`Kerk en kerkonderdeel`, 499 in de bbox). (4) De afstandsdrempels voor molen/kasteel/kerk in de statistiekenberekening gingen van 250/500/1000m naar 25/50/100m ("> 100 is niet interessant genoeg") -- molen nu 1/4/8 begraafplaatsen binnen 25/50/100m, kerk 125/141/151. Bleek achteraf niet wat Joop bedoelde: hij doelde op de bestaande rijksmonument-afstandsslider in de viewer zelf (`#rm-threshold`, "Rijksmonument op/nabij terrein"), die tot dan toe 50-250m liep in stappen van 50. `<input type="range">` ondersteunt geen onregelmatige stapgrootte, dus de slider loopt nu over een index (0/1/2, `min=0 max=2 step=1`) die in `src/app.js` naar de echte meterwaarde (25/50/100) wordt vertaald via `RM_THRESHOLD_STEPS`; alle andere code (predicates, label, permalink-parameter `rmt`) blijft gewoon in meters rekenen. Een oude permalink met een inmiddels niet meer bestaande waarde (bv. 150m) valt terug op de dichtstbijzijnde geldige stap i.p.v. de slider stuk te laten gaan.
- **kandidaat-begraafplaatsen (2026-08-27, EXPERIMENTEEL, wens van Joop)**: "waar zou je een begraafplaats verwachten die nog niet in de dataset staat?" Begonnen als "kerken zonder begraafplaats in de buurt" (statistisch te zwak signaal gebleken -- de meeste kerken zonder kerkhof zijn gewoon stedelijke/naoorlogse kerken, sinds 1829 mag er sowieso niet meer bij de kerk begraven worden). In plaats daarvan doorzoekt `scripts/compute_kandidaat_begraafplaatsen.py` de `omschrijving`-tekst van alle 22.254 archeologische onderzoeksgebieden (al in huis, geen nieuwe data) op ondubbelzinnige grooftermen (`begraafplaats`/`kerkhof`/`grafveld`/`grafkuil`/`grafheuvel`/`skeletresten`/`crematieresten`/`crematiegraf`/`inhumatiegraf`/`necropool`/`grafmonument`/`dodenakker`/`urnenveld`) -- `graven`/`begraven`/`begraving` bewust uitgesloten, bleken bij steekproef overwegend fout-positief (`graven` = vaak "opgraven", `begraven bodem` = een geologische term, geen mensenbegraving). `beenderen` stond er eerst ook in, maar kwam maar 2x in de hele dataset voor -- 1x "beenderen van een onvolgroeid rund" (Joops observatie, terecht: dierenbotten tellen niet mee) en 1x een menselijke vondst die via `begraafplaats` al apart gevonden werd -- dus netto alleen een fout-positief, geen enkele nieuwe echte treffer; verwijderd. 199 treffers, 43 daarvan buiten Zuid-Holland (zelfde bbox-rand-effect als bij de kerk-analyse, hier ook uitgefilterd via de gemeentegrenzen), 156 kandidaten. Elke kandidaat krijgt een ruwe zekerheidsindicatie (hedge-taal als "mogelijk"/"zou"/"verwacht" herkend -- 72 "concreet genoemd") en de afstand tot de dichtstbijzijnde al bekende begraafplaats. Sterkste vondst tot nu toe: "Jacobswoude", een verdwenen middeleeuwse nederzetting met kerk én kerkhof, twee keer apart teruggevonden in de tekst; ook een "grafveld" (Nieuwe tijd) bij Rhijnhof in Leiden, 1,4 km van de bekende begraafplaats daar, is een kandidaat die het bekijken waard is. Bekende valkuilen (allemaal expliciet genoemd op de pagina zelf): "Kerkhof" is ook een achternaam van onderzoekers (echt gebeurd, zie de treffer met "Marjolein Kerkhof"); een "grafveld" kan prehistorisch/Romeins zijn i.p.v. een begraafplaats zoals Leons dataset die verzamelt; hedge-taal-detectie is zelf ook maar een woordenboek-trucje. Nieuwe pagina `src/kandidaten.html`/`kandidaten.js`, met een onmiskenbare waarschuwingsbox bovenaan, alleen gelinkt vanaf de statistiekenpagina (niet vanaf het hoofdpaneel) om het duidelijk gescheiden te houden van de geverifieerde content. Elke rij heeft een link naar de brontekst (`cho_uri`) en naar de locatie op de hoofdkaart (via het permalink-systeem, met de onderzoeksgebieden-laag alvast aangevinkt). Niets hiervan is een claim dat er een begraafplaats is -- elke kandidaat moet door Leon beoordeeld worden.
- **kloosters en synagoges als kandidaat-signaal (2026-08-27, EXPERIMENTEEL, wens van Joop)**: aanvulling op de kandidaat-begraafplaatsen-pagina, uit dezelfde brainstorm ("kerken zijn de eerste kandidaten, wat zou nog meer kunnen zijn?"). Andere bron dan de tekstzoekactie: `rijksmonumenten.geojson`, gefilterd op `oorspronkelijke_functie_kort` bevat `klooster` resp. `synagoge`. Zelfde bbox-fetch kanttekening als bij de kerk-analyse (`scripts/analyse_spatial.py`) -- ook hier eerst filteren op de echte ZH-gemeentegrenzen: 13 van de 34 kloosters en 1 van de 4 synagoges vielen af (buiten de provincie). Complexen met meerdere apart geregistreerde rijksmonumentnummers (bv. losse kloostervleugels, zoals "De Hof" in Dordrecht met 8 nummers) worden geclusterd binnen 250m tot 1 kandidaat, anders had je dubbele rijen voor hetzelfde fysieke gebouw gekregen -- resultaat: 13 kloostercomplexen, 3 synagoges. Belangrijk verschil tussen de twee categorieen, expliciet genoemd in de eigen waarschuwingstekst per sectie: bij kloosters is een grote afstand tot een bekende begraafplaats een aanwijzing (kloosters hadden vaak een eigen begraafplaats, veel al tijdens/na de Reformatie opgeheven en gesloopt/herbestemd, dus een gemiste registratie is aannemelijk) -- bij synagoges betekent een grote afstand juist NIETS, want een joodse begraafplaats hoort van oudsher bewust NIET dicht bij de synagoge te liggen (vaak buiten de bebouwde kom, op een afgelegen plek; vgl. Joops opmerking over het toponiem "Jodenberg(je)"). De sterkste kloosterkandidaat: "De Hof" in Dordrecht, 1,17 km van de dichtstbijzijnde bekende begraafplaats. Elke rij heeft een link naar het monumentenregister en naar de locatie op de hoofdkaart (met de rijksmonumentenlaag alvast aangevinkt, inclusief "toon alle" zodat het punt niet wegvalt achter de rm-afstandsslider -- deze kandidaten liggen immers vaak juist ver van een bekende begraafplaats). Zelfde experimentele framing en scheiding van de geverifieerde content als de rest van de pagina.
- **kapellen als kandidaat-signaal (2026-08-27, EXPERIMENTEEL, wens van Joop)**: derde categorie in dezelfde `rijksmonument_kandidaten()`-functie als kloosters/synagoges, `oorspronkelijke_functie_kort` bevat `kapel`. Zelfde bbox-filtering en clustering -- 5 van de 19 kapellen vielen af (buiten ZH), 14 in_ZH geclusterd tot 13. Anders dan klooster/synagoge is "kapel" zelf geen eenduidige categorie: erin zitten 1 `Grafkapel` (een grafmonument dat per definitie al bij een begraafplaats hoort -- landde dan ook logisch onderaan, 6,4m van een bekende bp) en 1 `Bedevaartkapel`/1 `Bidkapel` (wegkapellen voor gebed/bedevaart, geen duidelijke begraaftraditie). Geen van beide handmatig uitgefilterd -- zelfde principe als de functiefilter in `app.js` ("geen vaste curatie", zie `docs/data/004-rce-mcp-querystrategie.md`): laat zien wat er is, laat Leon oordelen. Om dat mogelijk te maken kreeg `rijksmonument_kandidaten()` een nieuw `functie`-veld (de originele `oorspronkelijke_functie_kort`-waarde(n) in de cluster) -- met terugwerkende kracht ook zichtbaar bij kloosters/synagoges als "Type"-kolom. Sterkste kapel-kandidaat: een naamloze kapel in Delft, 735m van de dichtstbijzijnde bekende begraafplaats.
- **kadaster-bebouwingscheck op klooster/synagoge/kapel (2026-08-27, EXPERIMENTEEL, wens van Joop)**: "is de plek nu nog open terrein of allang volgebouwd?" -- de vraag zelf gaf Joop het antwoord alvast mee: "het kan zijn dat al gebouwd is". `bebouwing_bij_punt()` in `scripts/compute_kandidaat_begraafplaatsen.py` bevraagt live de Kadaster Kennisgraaf (KKG, SPARQL-endpoint `api.labs.kadaster.nl`, `imxgeo:Gebouw`/`ext:maaiveldgeometrie` via GeoSPARQL `geof:sfIntersects`) op het aantal BAG-gebouwen binnen 30m van elke klooster/synagoge/kapel-kandidaat, plus het oudste/nieuwste bouwjaar. Uitkomst: "De Hof" (Dordrecht, 1170m, de sterkste kloosterkandidaat) blijkt inderdaad dicht bebouwd -- 19 gebouwen binnen 30m, bouwjaren 1275-1989, midden in de historische Dordtse binnenstad. Ook de andere kandidaten met de grootste afstand tot een bekende begraafplaats (Leiden 1164m: 23 gebouwen; Schiedam 1052m: 6 gebouwen) staan in dichtbebouwd historisch centrum -- geen van de "verste" kandidaten bleek open terrein. Van de kloosters sprong er 1 uit met maar 1 gebouw binnen 30m: Sint Bavo (Noordwijk, 223m, 1 gebouw uit 1918) -- op de kapellenlijst waren dat er 3 (Alphen aan den Rijn 645m en 57m, Voorne aan Zee 164m). Die zijn het interessantst om als eerste te bekijken. Puur extra context, geen bevestiging: staat er niets, dan is dat een aanwijzing (het monument zelf staat er altijd al, dus "0" komt sowieso nooit voor); staat er wel iets, dan kan een kerkhof er evengoed nog liggen als tuin/plein naast het gebouw. Draait als losse stap binnen hetzelfde script (29 kandidaten x een live geo-query, ~15 minuten, vandaar `--no-kadaster-check` om 'm over te slaan), niet als onderdeel van de reguliere build -- eenmalige handmatige run, het resultaat wordt als statisch JSON gecommit. Nieuwe kolom "Gebouwen binnen 30m" op de klooster/synagoge/kapel-tabellen van `src/kandidaten.html`.
- **CHS-archeologielaag van Provincie Zuid-Holland (2026-08-27, wens van Joop, gevonden via https://data.overheid.nl/dataset/32677)**: nieuwe, toggelbare kaartlaag op de hoofdkaart -- "Archeologische terreinen van provinciaal belang", 662 polygonen. Andere bron dan de bestaande RCE-lagen: de eigen Cultuurhistorische Hoofdstructuur (CHS)-WFS-dienst van Provincie Zuid-Holland (`geodata.zuid-holland.nl/geoserver/cultuur/wfs`, laag `CHS_2016_ARCHEOLOGIE_PROVINCIAAL_BELANG`, publiek domein/CC0-achtig). Voordeel t.o.v. de RCE-data: eigen provinciale dienst, dus geen landelijke bbox-fetch en dus ook geen bbox-rand-effect (de dienst dekt per definitie alleen Zuid-Holland). `scripts/fetch_chs_archeologie.py` haalt 'm op (WGS84 direct via `srsName=EPSG:4326`, geen reprojectie nodig), schoont interne bronregistratie-/verwerkingsvelden op (`GRENS_SRC`, `VERWERKT`, het altijd lege `GDB_GEOMATTR_DATA`) en bewaart `MONUMENTNR`/`Gemeente`/`Plaats`/`Toponiem`/`Datering`/`WAARDE`/`Zichtbaarh`/`Beschrijving` -> `data/zuid-holland/chs-archeologie-provinciaal-belang.geojson` (464KB). Lazy-loaded net als de gemeentegrenzen/RCE-onderzoeksgebieden (standaard uit, referentielaag), eigen kleur `#997404` (olijfgeel/goud) om te onderscheiden van de al gebruikte `#e8590c` (rijksmonument archeologisch) en `#0c8599` (RCE-onderzoeksgebieden) -- belangrijk met het oog op Rene (kleurenblind). Bij het doorzoeken van het `Beschrijving`-veld met dezelfde grafterm-patroon als de kandidaat-begraafplaatsen-pagina kwamen meteen sterke nieuwe leads naar boven (bv. "Begraafplaats Oud-Kralingen" in Rotterdam, en een verdronken dorp "Cruyskerk/Erkentrudenkerke" met mogelijk bijbehorende begraafplaats in Dordrecht) -- nog niet verwerkt in de kandidatenpagina zelf, dat volgt als aparte stap (vierde bron, na klooster/synagoge/kapel).
- **CHS-archeologie als vierde kandidaat-signaal (2026-08-27, wens van Joop: "als tweede als 4e bron")**: dezelfde CHS-data als de nieuwe kaartlaag (hierboven), nu ook als vierde bron op de kandidaat-begraafplaatsen-pagina. Andere aanpak dan de eerste drie bronnen: geen gemeentegrenzen-filter nodig (provinciale dienst, geen bbox-rand-effect), en het grafterm-patroon wordt gezocht in de kortere `Toponiem`/`WAARDE`/`Beschrijving`-velden (in die volgorde) i.p.v. lange rapporttekst -- minder kans op fout-positieven zoals "Kerkhof" als onderzoekersachternaam, maar geen garantie: er zitten ook Romeinse/prehistorische grafvelden tussen, geen begraafplaats zoals Leons dataset verzamelt. 15 kandidaten uit 662 terreinen, waaronder de eerder gesignaleerde sterke leads ("Begraafplaats Oud-Kralingen" in Rotterdam, het verdronken dorp Cruyskerk/Erkentrudenkerke bij Dordrecht). Geen directe brontekst-link beschikbaar (deze open dataset publiceert geen detailpagina per record) -- elke rij heeft in plaats daarvan alleen een kaartlink, met de nieuwe CHS-laag alvast aangevinkt. Kan overlappen met kandidaten die al via de RCE-archeologie-tekstzoekactie gevonden zijn (andere bron, dus niet automatisch ontdubbeld).
- **aan/uit-toggle gebouwde vs archeologische rijksmonumenten (2026-08-27, wens van Joop: "kun je nog een aan uit button toevoegen links met gebouwde monumenten en archeologische monumenten (ceo:monumentAard)")**: twee nieuwe sub-checkboxes onder "Rijksmonumenten" in het paneel (`toggle-monumenten-gebouwd`/`toggle-monumenten-archeologisch`, beide standaard aan -- zelfde gedrag als voorheen). `monument_aard` bleek al aanwezig in `data/rce/rijksmonumenten.geojson` (14.105 `onroerend gebouwd`, 99 `archeologisch`, uit `ceo:heeftMonumentAard` via `queries/rce/rijksmonumenten.sparql`) en al gebruikt voor de puntkleur (blauw/oranje) en de vlak-vs-punt-weergave -- alleen nog geen manier om een van de twee uit te zetten. `updateMonumentenFilter()` in `src/app.js` voegt nu een `["in", ["get","monument_aard"], ["literal", [...]]]`-clausule toe zodra niet beide aanstaan (lege literal-array bij allebei uit, wat voor elke feature `false` oplevert); bij beide aan blijft de filter ongewijzigd (geen extra clausule, identiek aan de oude situatie). De twee bestaande legenda-items ("Rijksmonument (gebouwd, punt)"/"(archeologisch, vlak/punt)") zijn omgehangen van de brede `toggle-monumenten`-checkbox naar deze twee specifieke -- `updateLegendActivity()` kreeg daarvoor een `data-parent`-mechanisme zodat zo'n item ook dimt wanneer de bredere oudercheckbox (hier: "Rijksmonumenten" zelf) uitstaat, niet alleen de eigen checkbox. Permalink-codes `monbouwd`/`monarch` toegevoegd aan `LAYER_TOGGLE_CODES`, zelfde generieke mechanisme als de rest.
- **statistiekenpagina bijgewerkt met monument_aard (2026-08-27, wens van Joop: "laat de statistiekenpagina ook even bijwerken")**: kleine aanvulling op de bestaande "Rijksmonumenten in de buurt"-sectie, geen nieuwe eigen sectie. `monument_aard`-verdeling van alle 14.204 rijksmonumenten in de bbox (los van nabijheid tot een begraafplaats) toegevoegd aan `rijksmonumenten_stats` in `scripts/compute_statistics.py`: 14.105 `onroerend gebouwd`, 99 `archeologisch` -- zelfde getallen als de nieuwe aan/uit-toggle op de kaart (hierboven), nu ook zichtbaar op de statistiekenpagina. Bewust NIET de experimentele klooster/synagoge/kapel/CHS-archeologie-kandidaten hierheen gehaald: die horen thuis op de losse experimentele kandidatenpagina, niet tussen de "vertrouwde" cijfers -- dezelfde scheiding die de rest van deze sessie ook aanhoudt.
- **verdwenen begraafplaatsen, kaartlaag (2026-08-27, wens van Joop na een KMZ van Leon: "wat een mooi gegeven")**: Leon deelde `data/Verdwenen.kmz`, 907 punten landelijk met begraafplaatsen die niet meer bestaan -- geen automatisch signaal zoals de kandidatenpagina, maar Leons eigen historische kennis. Andere aard dan al het andere op de kaart: alleen een punt (geen terreinpolygoon, er is niets meer om te tonen) en niet experimenteel. `scripts/build_verdwenen_begraafplaatsen.py` filtert tot Zuid-Holland via de provinciepolygoon (907 -> 92) en berekent een ruwe `in_hoofddataset`-heuristiek (zelfde plaats + woordoverlap in de naam, na het strippen van generieke woorden als "begraafplaats"/"kerkhof"/"oude"; met een aliaslijst voor het enige gevonden schrijfwijzeverschil, `'s-Gravenhage` -> `Den Haag`, waar de hoofddataset zelf altijd "Den Haag" gebruikt) -> `data/generated/verdwenen-begraafplaatsen.geojson`. 14 van de 92 lijken al bekend (vaak als "geruimd"), 78 zijn dat niet -- waaronder sterke aansluitingen bij de rest van vandaag: meerdere "Joodse begraafplaats"/"Portugees-Joodse begraafplaats", diverse kloosterkerkhoven ("Kerkhof Agnietenklooster"/"Kerkhof Barbaraklooster"/"Kloosterkerkhof"), en "Pestkerkhof" in zowel Delft als Rotterdam (relevant na de eerdere gasthuizen/pesthuizen-zoektocht die met de beschikbare RCE-data niets opleverde). Nieuwe laag `toggle-verdwenen` op de hoofdkaart (lazy, 92 punten), rood voor de 78 "onbekend/mogelijk ontbrekend" (het interessantste deel) en grijs voor de 14 "lijkt al bekend", met een expliciete disclaimer in zowel de data (`waarschuwing`-veld) als de popup dat `in_hoofddataset` een ruwe heuristiek is, geen exacte koppeling.

## Reproduceerbaarheid

De volledige keten is nu werkende code, geen handmatige snapshot meer:

```text
CSV (data/Begraafplaatsen Zuid-Holland- Zuid-Holland.csv)
  -> scripts/build_base_dataset.py  -> data/generated/begraafplaatsen.geojson + .csv
  -> scripts/fetch_rce.py           -> data/rce/*.geojson
  -> scripts/analyse_spatial.py     -> data/generated/analyse.geojson
  -> src/index.html + app.js        -> viewer
```

`build_base_dataset.py` implementeert de matchingregels uit
[003 CSV als genormaliseerde bronlaag](data/003-csv-bron-en-koppeling.md) en
faalt met een assertion zodra de bekende invarianten (891/446/445,
434/11/1/0 koppelwijzen, 0 statusconflicten) niet meer kloppen. Geverifieerd
tegen de eerder handmatig gebouwde snapshot: identieke uitkomst, op de
opmaak van `bron_rij_ingang` na (voorheen `18.0`, nu `18` -- het brongegeven
was altijd een geheel getal).

## Hosting

Live voor Leon op **https://dodenakkers-zh.pages.dev/** (Cloudflare Pages,
project `dodenakkers-zh`, account jolietjakeblues64@gmail.com). Gekozen
boven GitHub Pages omdat dat account al draait voor doorzoekerfgoed.nl —
zie [006 Hosting](ideas/006-hosting.md) voor de oorspronkelijke afweging
(die inmiddels is herzien).

`site/` is een build-artefact (gitignored, niet committen) dat alleen bevat
wat de viewer nodig heeft - niet de hele repo.

`scripts/build_site.py` kopieert `src/index.html`/`style.css`/`app.js` en de
drie GeoJSON-bestanden die de viewer nodig heeft naar `site/`, en herschrijft
`app.js`'s `../data/...`-paden naar `data/...` (index.html staat in `site/`
op het root-niveau, niet onder `src/`).

### Automatische deploy (2026-08-23)

De Cloudflare Pages-project `dodenakkers-zh` is direct aan deze GitHub-repo
gekoppeld (Cloudflare's eigen Git-integratie, hetzelfde patroon als
doorzoekerfgoed.nl). Cloudflare bouwt en deployt zelf bij elke push naar
`main` — build command `python scripts/build_site.py`, output directory
`site`. Geen GitHub Action, geen API-token, geen handmatig `wrangler`-
commando meer nodig. Zie
[006 Hosting](ideas/006-hosting.md#automatische-deploy-2026-08-23) voor de
aanloop (een eerdere, inmiddels verwijderde poging met een GitHub Action +
API-token werkte ook, maar was nodeloos ingewikkeld vergeleken met deze
Git-integratie).

Handmatig blijft ook mogelijk (bv. om een niet-main-branch te testen):

```bash
python scripts/build_site.py
npx wrangler pages deploy site --project-name dodenakkers-zh --branch main
```

## Nummering

Nieuwe ideeën krijgen een oplopend nummer zodat beslissingen en denkrichtingen later goed terug te vinden zijn.
